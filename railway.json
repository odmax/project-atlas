{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p bootstrap/cache storage/logs storage/framework/cache/sessions storage/framework/views storage/framework/cache/data && npm install && npm run build && php artisan config:clear && php artisan cache:clear"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}